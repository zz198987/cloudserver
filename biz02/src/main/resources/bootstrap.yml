eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:8761/eureka"
spring:
  application:
    name: biz02
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        service-id: configserver
        enabled: true